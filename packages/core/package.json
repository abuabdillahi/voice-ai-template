{
  "name": "@sarjy/core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"core: no dev task\"",
    "build": "echo \"core: no build task\"",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "format": "uv run ruff format .",
    "test": "uv run pytest -m \"not integration\"",
    "typecheck": "uv run mypy ."
  }
}
