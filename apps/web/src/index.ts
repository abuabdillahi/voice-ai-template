// Placeholder entrypoint. The Vite + React SPA is wired up in issue 04;
// this file exists so tsc and eslint have something to compile against
// before then. Safe to delete or replace once real source lands.
export {};
