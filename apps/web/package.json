{
  "name": "@voice-ai/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"web: dev stub (Vite wired up in issue 04)\"",
    "build": "mkdir -p dist && cp index.html dist/",
    "lint": "echo \"web: lint stub (real config arrives in issue 02)\"",
    "test": "echo \"web: test stub\"",
    "typecheck": "echo \"web: typecheck stub\""
  }
}
