# Issue 03: Docker scaffolding

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/voice-ai-template/PRD.md`

## What to build

Containerize each app and provide both a development and a production compose file. After this slice, `docker compose build` succeeds for all three services, `docker compose up` brings up the dev stack with stubs that respond to a healthcheck, and CI verifies all three Dockerfiles still build on every PR. The web service in production is served as static assets through nginx; in development it runs outside compose via Vite for HMR.

LiveKit is not yet wired (that arrives in issue 05) — this slice establishes only the containerization shape.

## Acceptance criteria

- [ ] `apps/api/Dockerfile` is multi-stage (uv-based builder → `python:3.13-slim` runtime), runs as non-root, exposes port 8000, has a `HEALTHCHECK` hitting `/health`
- [ ] `apps/agent/Dockerfile` is multi-stage (uv-based builder → `python:3.13-slim` runtime), runs as non-root, no exposed ports (dial-out only)
- [ ] `apps/web/Dockerfile` is multi-stage (pnpm-based builder running `pnpm build` → `nginx:alpine` runtime serving `dist/`)
- [ ] `apps/api` has a `/health` endpoint returning `{"status": "ok"}`; `apps/agent` has a stub entrypoint that logs a startup line and sleeps (real worker arrives in issue 05)
- [ ] `docker-compose.yml` at the repo root brings up `api` and `agent` services with `.env`-loaded configuration; web is excluded from dev compose with a comment explaining Vite runs outside
- [ ] `docker-compose.prod.yml` at the repo root adds the `web` (nginx) service and a placeholder for `livekit/livekit-server` (commented out until issue 05 wires it)
- [ ] `.dockerignore` files exclude `node_modules`, `.venv`, `dist`, `build`, `.git`, env files
- [ ] `.env.example` at the repo root lists all variables expected so far (Supabase URL/keys placeholders, future LiveKit/OpenAI placeholders are commented)
- [ ] `docker compose build` succeeds for all services on a clean clone
- [ ] `docker compose up` brings api and agent to a running state; api's `/health` returns 200 within 30s
- [ ] `.github/workflows/ci.yml` gains a `build` job that runs `docker buildx build` for each app's Dockerfile (no push), gated to run after `lint`
- [ ] README documents the dev (`pnpm dev` for web, `docker compose up` for api/agent) and prod (`docker compose -f docker-compose.prod.yml up`) workflows

## Blocked by

- Issue 01 (monorepo skeleton)

## Comments

> *This was generated by AI during triage.*

**2026-05-04 — Bulk-promoted to `ready-for-agent`.** Maintainer reviewed the full set of 11 issues produced by `/to-issues` and approved them all for AFK pickup in one pass. The issue body above is treated as the agent brief; no separate brief was authored. Category labelled `enhancement`. Implementation order is governed by the `Blocked by` field — start by acquiring the issue with no remaining blockers, then move down the dependency chain.

**2026-05-04 — Implemented.** All acceptance criteria met. Added `fastapi` + `uvicorn[standard]` to `apps/api`, a `/health` route in `apps/api/api/main.py`, and a JSON-logging stub at `apps/agent/agent/__main__.py`. Three multi-stage Dockerfiles (api/agent share an identical uv-builder → `python:3.13-slim` shape; web is `node:22-alpine` → `nginx:alpine`); api runs as non-root `appuser`, exposes 8000, and ships a `python -c urllib.request` healthcheck so we don't pull `curl` into the slim runtime. `docker-compose.yml` brings up api + agent only (web runs outside compose for Vite HMR — explicit comment in the file); `docker-compose.prod.yml` adds the web nginx service plus a commented-out `livekit-server` block reserved for issue 05. `.env.example` lists Supabase placeholders with LiveKit/OpenAI commented for the future slice. CI workflow is a single `build` matrix job (api/agent/web Dockerfiles via `docker/build-push-action@v6`, no push) — defined as `build` only because issue 02 is concurrently adding `lint` in a parallel worktree; a comment in the workflow notes the `needs: [lint]` wiring is a follow-up after both branches merge.

Deviations: (1) Dockerfiles share a per-app `.dockerignore` plus a repo-root `.dockerignore`; the root one is what BuildKit actually reads since the build context is the repo root (api/agent need workspace-level `pyproject.toml`/`uv.lock` and `packages/core/`). The per-app files remain for IDE/tooling discovery and to satisfy the AC literally. (2) Used `docker/build-push-action@v6` with a strategy matrix instead of three explicit `docker buildx build` shell invocations — same effect, GHA cache integration as a bonus. (3) `apps/web/package.json`'s `build` script is a `mkdir -p dist && cp index.html dist/` shim until the proper Vite build lands in issue 04, as instructed.

Verification: `uv sync --all-packages` resolves the new deps cleanly (22 packages, fastapi 0.136.1, uvicorn 0.46.0). `uv run python -c "from api.main import app; print(app)"` imports the FastAPI app. `pnpm exec turbo run build` succeeds across all four packages and emits `apps/web/dist/index.html`. Docker daemon was unreachable in the sandbox (`permission denied` on the Docker socket), so `docker compose build` couldn't be run end-to-end; instead validated both compose files via `docker compose config` (parses cleanly with all services, ports, and the python-urllib healthcheck rendered correctly). Dockerfile syntax was reviewed manually — they use BuildKit `# syntax=docker/dockerfile:1.7` with `--mount=type=cache` directives, standard multi-stage `COPY --from=builder` patterns, and non-root user creation via `groupadd`/`useradd`.
