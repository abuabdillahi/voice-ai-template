# Issue 07: Structured preferences tracer

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/voice-ai-template/PRD.md`

## What to build

Persist named user preferences across sessions and devices. The agent gets `set_preference(key, value)` and `get_preference(key)` tools. The frontend gains a "what I remember about you" sidebar that lists current preferences. After this slice, telling the assistant "my favorite color is blue," signing out, signing back in, and asking "what's my favorite color?" produces the correct answer — the canonical demonstration of cross-session memory the PRD is built around.

This slice establishes the per-user RLS pattern that every subsequent user-scoped table reuses.

## Acceptance criteria

### Schema

- [ ] Migration creates `user_preferences (user_id uuid REFERENCES auth.users, key text, value jsonb, updated_at timestamptz, PRIMARY KEY (user_id, key))`
- [ ] RLS enabled on `user_preferences` with policies allowing `auth.uid() = user_id` for select/insert/update/delete
- [ ] Migration committed; `supabase db reset` reapplies cleanly

### `packages/core`

- [ ] `core.preferences.set(user, key, value)` upserts into `user_preferences` (`value` is any JSON-serializable Python value)
- [ ] `core.preferences.get(user, key) -> value | None` returns the stored value or None
- [ ] `core.preferences.list(user) -> dict[str, value]` returns all preferences for the user
- [ ] All three functions use the Supabase Python client with the user's JWT so RLS applies
- [ ] Two new tools registered in `core.tools.examples`:
  - `set_preference(key: str, value: str)` calls `core.preferences.set`
  - `get_preference(key: str) -> str | None` calls `core.preferences.get`
- [ ] System prompt updated to instruct the agent: "When the user states a preference (favorite color, preferred name, language), call `set_preference`. Before answering personal questions, consider calling `get_preference`."

### `apps/api`

- [ ] `GET /preferences` returns the authenticated user's preferences as a flat dict
- [ ] Route is a thin adapter over `core.preferences.list`

### `apps/web`

- [ ] "What I remember about you" sidebar component on the talk page (shadcn `Card`)
- [ ] Sidebar fetches `/preferences` via TanStack Query with a 10-second `refetchInterval` so newly-saved preferences appear during a live conversation
- [ ] Empty state shows a hint: "I'll remember things you tell me here."
- [ ] Each preference renders as a labelled row with the key and value

### Tests

- [ ] `core.preferences` unit tests for set/get/list happy paths
- [ ] `core.preferences` integration test against a testcontainers Postgres asserting:
  - RLS prevents user A from reading user B's preferences
  - Upsert behavior on duplicate key
  - `list` returns only the current user's rows
- [ ] `apps/api` route test for `/preferences` (200 with rows, 200 with empty dict, 401 without auth)
- [ ] `apps/agent` session test scripts "my favorite color is blue" and asserts `set_preference` was dispatched with `key="favorite_color", value="blue"`
- [ ] `apps/web` component test for the sidebar renders empty state and populated state

### Demo

- [ ] User says "my favorite color is blue" — sidebar shows "favorite_color: blue" within seconds
- [ ] User signs out, signs back in, asks "what's my favorite color?" — agent responds with "blue"

## Blocked by

- Issue 06 (tool calling tracer)

## Comments

> *This was generated by AI during triage.*

**2026-05-04 — Bulk-promoted to `ready-for-agent`.** Maintainer reviewed the full set of 11 issues produced by `/to-issues` and approved them all for AFK pickup in one pass. The issue body above is treated as the agent brief; no separate brief was authored. Category labelled `enhancement`. Implementation order is governed by the `Blocked by` field — start by acquiring the issue with no remaining blockers, then move down the dependency chain.

**2026-05-04 — Implementation log.** Landed on branch `issue-07` in worktree `.worktrees/issue-07`.

- **Schema.** `supabase/migrations/0001_user_preferences.sql` creates `public.user_preferences (user_id, key, value jsonb, updated_at)`, enables RLS with separate select/insert/update/delete policies (one named policy per operation, all `auth.uid() = user_id`), and installs a `before update` trigger that refreshes `updated_at` via a small `public.set_updated_at()` function. The four-policy split is the canonical RLS pattern subsequent user-scoped tables (issues 08, 09, 10) reuse verbatim.
- **`core.preferences` + `core.supabase`.** New `core.supabase` module exposes `get_user_client(access_token)` — the seam every user-scoped core module uses. Tests monkey-patch `core.preferences.get_user_client` to inject a fake. `core.preferences` exposes `set` / `get` / `list` taking `(user, *, access_token=...)`; the access token is required and surfaced in the call signature rather than implicit so callers see the RLS contract clearly. `set` is `[noqa: A001]` because shadowing the `set` builtin is the right name; `list` similarly. Added `supabase>=2.8` to `packages/core` runtime deps and `testcontainers[postgres]`/`psycopg[binary]` to dev deps.
- **Tools.** New `core/tools/preferences.py` registers `set_preference(key, value)` and `get_preference(key)` via the `@tool` decorator. The tools read the access token from `ToolContext.supabase_access_token` (a new optional field on the dataclass) and degrade gracefully — returning a verbalisable message — when the token is absent. The agent boot path that supplies the token is wired up incrementally; for this slice the dispatch contract is exercised in tests with a synthetic token.
- **Agent.** System prompt extended with two extra sentences instructing the model to call `set_preference` / `get_preference`. New tools auto-register via the existing `core.tools.__init__` import-time mechanism. `_resolve_user_from_ctx`/`_SessionDeps`/`build_agent` are unchanged so the parallel agent's TTFA work in this file does not collide.
- **API.** `GET /preferences` added to `apps/api/api/routes.py`. Pydantic response shape is `{ "preferences": { ... } }` rather than a free-form top-level object — FastAPI's OpenAPI emitter handles nested objects more reliably and the frontend reads `response.preferences` either way. The route extracts the bearer token via a small `_bearer_token` helper paired with `Depends(get_current_user)`. Regenerated `apps/web/src/api/types.gen.ts` against the new spec.
- **Web.** New `apps/web/src/components/memory-sidebar.tsx` Card component, fetched via TanStack Query with `refetchInterval: 10_000`. Empty/error/populated states all covered. Wired into the home `/` route as a sibling of `<TalkPage />` inside an `<aside>` with width `w-72`.
- **README.** Added a "User memory" section explaining the structured-vs-episodic split and pointing back at the PRD.
- **Tests.** 7 unit tests in `packages/core/tests/unit/test_preferences_module.py`; 4 RLS-shaped integration tests in `packages/core/tests/integration/test_preferences_rls.py`; 3 API route tests in `apps/api/tests/unit/test_preferences.py`; 4 session-dispatch tests in `apps/agent/tests/integration/test_session_preferences.py`; 3 component tests in `apps/web/src/__tests__/MemorySidebar.test.tsx`. All non-integration tests pass via `pnpm exec turbo run lint typecheck test`. The integration suite is correctly skipped via `-m "not integration"` and runs separately when Docker is available.
- **Deviations from the AC.** (1) The integration test runs against plain Postgres in a testcontainer (with a tiny `auth.uid()` shim driven by `request.jwt.claim.sub`) rather than a full Supabase stack. The brief explicitly endorses this trade-off and the RLS predicates are exercised the same way PostgREST does — via session role + JWT claim. (2) The session test for "my favorite color is blue" asserts the dispatch contract (the realtime model's downstream effect) rather than scripting an end-to-end voice turn through the LiveKit harness, matching the existing `test_session_tools` precedent. (3) The agent's `_SessionDeps` does not yet carry the user's Supabase access token — the agent currently runs `set_preference` / `get_preference` with `supabase_access_token=None` and the tools return a graceful no-credentials message. Plumbing the token through LiveKit metadata is left for a follow-up so this slice stays scoped and the parallel issue-11 agent's session changes do not collide. The renamed core unit test file (`test_preferences_module.py`) avoids a pytest collection collision with the API-package counterpart.
