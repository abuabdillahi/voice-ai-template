# Issue 02: Linting, formatting, type-checking, hooks, CI lint job

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/voice-ai-template/PRD.md`

## What to build

Wire up the full code-hygiene pipeline so that contributors cannot commit unformatted, unlinted, or improperly-typed code, and so that the same checks run in CI. Two-tier hook split — fast formatters/linters/secret scan on commit, slower type-checkers and unit tests on push. Conventional-commit format enforced from the first commit.

After this slice, `pre-commit run --all-files` passes on a fresh clone, a deliberately broken commit message is rejected, and the CI lint job runs on pull requests.

## Acceptance criteria

- [ ] `pyproject.toml` at the repo root contains ruff and mypy configuration; ruff selects an opinionated rule set; mypy is set to strict
- [ ] `eslint.config.js` (flat config) at the repo root configures `@typescript-eslint`, `eslint-plugin-react`, `eslint-plugin-react-hooks`
- [ ] `.prettierrc` at the repo root configures 2-space indent, single quotes, trailing commas
- [ ] `.editorconfig` at the repo root pins indent style, line endings, charset
- [ ] `.pre-commit-config.yaml` at the repo root configures:
  - commit-stage hooks: `ruff format`, `ruff check --fix`, `prettier --write`, `eslint --fix`, trailing-whitespace, EOF newline, merge-conflict marker check, large-file check, `detect-secrets` (or `gitleaks`)
  - push-stage hooks: `mypy`, `tsc --noEmit`, fast unit tests (`pytest -m "not integration"` and `vitest run --no-coverage`)
  - commit-msg-stage hook: commitlint with conventional config
- [ ] `commitlint.config.js` at the repo root extends `@commitlint/config-conventional`, sets subject max length to 72
- [ ] `.secrets.baseline` is committed at the repo root
- [ ] Each Python package has lint/format/typecheck commands wired through Turbo; each TS package the same with eslint/prettier/tsc
- [ ] A deliberately bad commit message (e.g. `bad message`) is rejected by the commit-msg hook
- [ ] `.github/workflows/ci.yml` runs a `lint` job on PR and push to main that reuses the same commands (ruff, mypy, eslint, prettier --check, tsc)
- [ ] CI uses `actions/setup-uv@v3`, `actions/setup-node@v4` with pnpm caching, and Turborepo remote cache (or local cache for now)
- [ ] README documents `pre-commit install` as a one-time setup step

## Blocked by

- Issue 01 (monorepo skeleton)

## Comments

> *This was generated by AI during triage.*

**2026-05-04 — Bulk-promoted to `ready-for-agent`.** Maintainer reviewed the full set of 11 issues produced by `/to-issues` and approved them all for AFK pickup in one pass. The issue body above is treated as the agent brief; no separate brief was authored. Category labelled `enhancement`. Implementation order is governed by the `Blocked by` field — start by acquiring the issue with no remaining blockers, then move down the dependency chain.

**2026-05-04 — Implemented.** All acceptance criteria met. Highlights:

- Root `pyproject.toml` now carries `[tool.ruff]` (line-length 100, target-version `py313`, the suggested rule set `E,F,I,N,UP,B,A,C4,PIE,RET,SIM,ARG`) and `[tool.mypy]` (`strict = true`, plus pretty/error-codes/warn-unreachable). A `dev` dependency-group adds ruff, mypy, pytest, pre-commit, and detect-secrets without touching the production dependency list. A small per-module override tolerates the empty stub packages until real source lands.
- Flat `eslint.config.js` wires `@eslint/js`, `typescript-eslint`, `eslint-plugin-react`, and `eslint-plugin-react-hooks`. Ignores the usual build/cache dirs so it runs cleanly against the empty `apps/web` tree. Root `.prettierrc` (2-space, single quotes, trailing-all, 100 cols, LF) and `.editorconfig` round out the formatting layer.
- Each Python package's `package.json` now runs `uv run ruff check . && uv run ruff format --check .` for `lint`, `uv run mypy .` for `typecheck`, and `uv run pytest -m "not integration"` for `test`. `apps/web` runs `eslint . --max-warnings 0` and `tsc --noEmit` against a new minimal `apps/web/tsconfig.json` plus an `apps/web/src/index.ts` placeholder so tsc has at least one input file before issue 04 lands the real SPA.
- `.pre-commit-config.yaml` declares three stages: `pre-commit` (ruff format, ruff check --fix, prettier, eslint, trailing-whitespace, end-of-file-fixer, check-merge-conflict, check-added-large-files, detect-secrets), `pre-push` (mypy, tsc --noEmit, pytest "not integration", vitest run --no-coverage), and `commit-msg` (commitlint via `pnpm exec commitlint --edit`). The pytest entry is wrapped to treat exit code 5 (no tests collected) as success so push hooks pass on the empty skeleton.
- `commitlint.config.js` extends `@commitlint/config-conventional` with `subject-max-length = 72`. `.secrets.baseline` was generated via `detect-secrets scan` (excluding lockfiles and caches).
- `.github/workflows/ci.yml` defines a single `lint` job on PR/push-to-main that uses `actions/setup-uv@v3` and `actions/setup-node@v4` with pnpm caching and runs ruff (lint + format check), mypy, eslint, prettier --check, and tsc --noEmit. Issue 03 will append the `build` job alongside it.
- README's "Getting started" picked up a "One-time hook setup" subsection documenting `pre-commit install` for all three hook types.

Verified: `uv sync --all-packages`, `pnpm install`, `pnpm exec turbo run lint` (4/4 successful), `pnpm exec turbo run typecheck` (4/4 successful), `pre-commit run --all-files` (clean), `pre-commit run --all-files --hook-stage pre-push` (clean), and `pre-commit run --hook-stage commit-msg --commit-msg-filename <path>` rejects a non-conventional message (rc=1) and accepts `feat: ok` (rc=0).

Deviations:
- Added a `apps/web/src/index.ts` placeholder (`export {};`) and a `.prettierignore` rule for `.scratch/`. Both were necessary to keep tsc/prettier exit-0 on the current empty skeleton; the placeholder is explicitly marked as safe-to-delete in issue 04.
- Set `"type": "module"` in the root `package.json` so `eslint.config.js` and `commitlint.config.js` can use ESM `export default` syntax (the canonical form in current docs).
- Issue spec mentions Turborepo remote cache as a CI option; the `lint` job uses Turbo's local cache only (no remote-cache secrets configured yet). Marked as "or local cache for now" in the AC, so within scope.
