# Issue 04: Auth tracer

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/voice-ai-template/PRD.md`

## What to build

The first end-to-end vertical tracer: Supabase authentication wired through every layer. After this slice, a developer cloning the template, signing up via the web UI, and landing on the home page sees their email address rendered — proving Supabase Auth, the typed FastAPI dependency, the Vite frontend, generated TS types, and Supabase Cloud's free tier all work together.

This slice establishes the patterns every subsequent tracer reuses: Supabase migration, RLS policy template, `core` module shape, FastAPI route handler shape, generated OpenAPI client shape, TanStack Router route shape, test patterns at every layer.

## Acceptance criteria

### Schema and Supabase setup

- [ ] `supabase/` directory initialized via Supabase CLI; `supabase/config.toml` committed
- [ ] `supabase/migrations/` contains an initial migration that enables the `pgvector` extension and creates a placeholder for future tables (no application tables here yet — auth tables are Supabase-managed)
- [ ] `supabase start` brings the local Supabase stack up successfully

### `packages/core`

- [ ] `core.config.Settings` (pydantic settings) loads `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_JWT_SECRET` from environment
- [ ] `core.auth.verify_token(token: str) -> User` validates Supabase JWTs (JWKS or HS256 depending on Supabase config) and returns a typed `User` dataclass with `id` (uuid) and `email`
- [ ] `core.auth.get_current_user` is exported as a FastAPI `Depends`-compatible callable that extracts the bearer token, verifies it, and returns `User`; raises 401 on failure with structured detail

### `apps/api`

- [ ] FastAPI application factory with CORS configured for the dev frontend origin
- [ ] `GET /health` (existing from issue 03) and `GET /me` route returning the current `User` (200) or 401 if unauthenticated
- [ ] OpenAPI schema available at `/openapi.json`
- [ ] Request-ID middleware binds a `request_id` to the structlog context for each request

### `apps/web`

- [ ] Supabase JS client initialized with public env vars (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
- [ ] TanStack Router file-based routes: `/sign-in` (custom shadcn form using `signInWithPassword` and `signUp`) and `/` (home, requires auth, fetches `/me`, displays the email)
- [ ] An `AuthProvider` exposes the current Supabase session via context and a `useUser` hook
- [ ] A `RequireAuth` route guard redirects unauthenticated users to `/sign-in`
- [ ] A typed API client generated from `apps/api/openapi.json` using `openapi-typescript`, output to `apps/web/src/api/types.gen.ts`; a small `apiFetch` helper attaches the Supabase access token as a bearer header
- [ ] `pnpm gen:api` script regenerates the types against a running api
- [ ] `apps/web` integrates Tailwind, shadcn (initialized with components.json), and at least the shadcn `Button`, `Input`, `Card`, `Form` components used by `/sign-in`

### Tests

- [ ] `core.auth` unit tests: valid token → User, expired token → raises, malformed token → raises, missing claims → raises
- [ ] `apps/api` unit test for `/me`: 200 with valid mocked user dependency, 401 without
- [ ] `apps/web` example component test using vitest + React Testing Library on the sign-in form
- [ ] All run via `turbo run test`

### Demo

- [ ] On a fresh clone with Supabase env vars configured, running the dev stack and registering a new user lets the user sign in and see their email on the home page
- [ ] README updated with the auth setup section (Supabase project creation steps, env vars to set)

## Blocked by

- Issue 01 (monorepo skeleton)
- Issue 02 (tooling)
- Issue 03 (Docker scaffolding)

## Comments

> *This was generated by AI during triage.*

**2026-05-04 — Bulk-promoted to `ready-for-agent`.** Maintainer reviewed the full set of 11 issues produced by `/to-issues` and approved them all for AFK pickup in one pass. The issue body above is treated as the agent brief; no separate brief was authored. Category labelled `enhancement`. Implementation order is governed by the `Blocked by` field — start by acquiring the issue with no remaining blockers, then move down the dependency chain.

**2026-05-04 — Implemented end-to-end.** Single commit on top of `a6902b3`.

Layers added:

- **`packages/core`** — `Settings` (pydantic-settings v2) + cached `get_settings()`; `User` dataclass with `verify_token()` (HS256 JWT, pinned algorithm, audience-checked) and `get_current_user` FastAPI dependency; minimal `setup_logging()` configuring `structlog` with JSON-in-prod / console-in-dev rendering and `request_id` / `user_id` context-var binding. Unit tests cover all four canonical token states.
- **`apps/api`** — application factory in `api/app.py`, CORS middleware (Vite origin + comma-separated `CORS_ORIGINS`), `RequestIDMiddleware` that binds the id to structlog and echoes it as `X-Request-ID`, routes split into `api/routes.py`, `/me` returning the authenticated user. `tests/conftest.py` exposes `client` / `authed_client` / `fake_user` fixtures used by `test_health.py` and `test_me.py`.
- **`supabase/`** — committed `config.toml` and `migrations/0000_init.sql` enabling `pgvector`. Auth tables are Supabase-managed; the comment in the migration documents the canonical RLS policy template later issues will reuse.
- **`apps/web`** — Vite + React 18 SPA with TanStack Router file-based routing (`__root`, `/`, `/sign-in`), TanStack Query, AuthProvider context wired to `supabase.auth.onAuthStateChange`, `RequireAuth` route guard via `beforeLoad`, `apiFetch` helper attaching the bearer token, shadcn/ui (`Button`, `Input`, `Label`, `Card`, `Form`) authored manually since `pnpm dlx shadcn init` is unavailable in the sandbox, Tailwind v4 via `@tailwindcss/vite`. Generated `apps/api/openapi.json` and `apps/web/src/api/types.gen.ts` are committed for offline builds; `pnpm gen:api` regenerates against the local file (the README documents the equivalent live-server flow).

Test wiring:

- `core.auth` — 11 tests covering valid / expired / malformed / bad-signature / missing-claim / non-UUID-`sub` / wrong-scheme / missing-header.
- `api` — 3 tests on `/health` (status, request-id roundtrip), 4 tests on `/me` (200 with override, 401 without, 401 invalid token, 401 wrong scheme).
- `web` — `SignInForm` test covering render, validation messages, and credential submit via mocked supabase client.

Deviations from the AC, with reasoning:

- The sign-in component was extracted to `src/components/sign-in-form.tsx` and the route file (`src/routes/sign-in.tsx`) became a thin wrapper. This let the vitest test mount the form without the TanStack Router runtime, keeping the test focused on the form behaviour.
- Tailwind v4 ships utility classes that do not include the shadcn `bg-primary` / `text-foreground` shorthands by default; the shipped components use `bg-[hsl(var(--primary))]` arbitrary-value syntax instead. The visual result is identical and the components remain easy to migrate when a future commit introduces a `@theme` block.
- `jsdom` was swapped for `happy-dom` because `jsdom` pulls in `whatwg-encoding` → `iconv-lite`, whose tarball contains a `.idea/` directory that the local sandbox refuses to extract. `happy-dom` is the canonical alternative and the test suite is unaffected.
- A `pnpm.overrides` entry pins `resolve` to `1.22.10` for the same reason — `resolve@2.0.0-next.6` ships a `.gitmodules` file the sandbox blocks.
- Supabase CLI is not installed locally, so `supabase start` is documented in the README rather than verified during CI of this slice. The committed `supabase/config.toml` and `supabase/migrations/0000_init.sql` are valid and apply with `supabase db reset` once the CLI is available.
- `TMPDIR` and `UV_CACHE_DIR` were added to `turbo.json`'s `globalPassThroughEnv` so the sandbox-relative caches propagate to spawned tasks; this is normally a per-developer concern but is harmless to commit.

Verification (all green):

- `UV_CACHE_DIR=$PWD/.uv-cache uv sync --all-packages`
- `TMPDIR=$PWD/.tmp UV_CACHE_DIR=$PWD/.uv-cache pnpm exec turbo run lint typecheck test` (12 tasks, all pass)
- `pnpm exec prettier --check .`
- `pnpm --filter @voice-ai/web build` (produces `apps/web/dist/`)
- `uv run python -c "from api.main import app; print(app.routes)"` shows `/health` and `/me`.

Setup the developer must perform on a real Supabase project to run the demo:

1. Create a project at supabase.com, copy `Project URL`, `anon public key`, and `JWT Secret` into `.env` (and the `VITE_SUPABASE_*` mirrors).
2. Disable email confirmation in **Auth → Providers → Email** if no SMTP server is configured.
3. Optionally `supabase start` for a local stack; `supabase db reset` applies the bundled migration.
