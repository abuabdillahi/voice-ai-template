# Issue 11: TTFA logging tracer

Status: ready-for-agent
Category: enhancement

## Parent

`.scratch/sarjy/PRD.md`

## What to build

Hook LiveKit Agents' `MetricsCollectedEvent` and emit a structured JSON log line per turn capturing server-side time-to-first-audio and component latencies (end-of-utterance delay, LLM time-to-first-token, TTS time-to-first-byte). Set up `structlog` with JSON output, request/conversation correlation IDs, and the per-turn metrics emission. After this slice, `docker logs` on the agent worker shows a queryable stream of latency data per turn.

This is the "minimum viable monitoring" the PRD agreed on. Heavier observability (Langfuse, dashboards, client-side TTFA) is deferred to a future iteration.

## Acceptance criteria

### `packages/core`

- [ ] `core.observability.setup_logging(level: str, json_output: bool)` configures `structlog`:
  - JSON output in production (default), pretty-printed in development
  - Standard processors: timestamp, log level, contextual binding
  - Compatible with stdlib `logging` consumers
- [ ] `core.observability.bind(**fields)` is a thin convenience over `structlog.contextvars.bind_contextvars`
- [ ] `core.observability.handle_metrics_event(event)` accepts a LiveKit `MetricsCollectedEvent` and emits a single structured log line at INFO level with:
  - `event = "turn_metrics"`
  - All numeric fields from the event (eou_delay, llm_ttft, tts_ttfb, total_latency, token counts where available)
  - The currently-bound contextvars (which include `conversation_id`, `session_id`, `user_id`)

### `apps/api`

- [ ] On startup, calls `core.observability.setup_logging`
- [ ] Existing request-id middleware now uses `core.observability.bind(request_id=...)` so subsequent log lines auto-include it

### `apps/agent`

- [ ] On startup, calls `core.observability.setup_logging`
- [ ] Worker subscribes to LiveKit Agents' metrics events and forwards each to `core.observability.handle_metrics_event`
- [ ] On session start (already in issue 09), binds `user_id`, `session_id`, `conversation_id` so those appear on every line for the session
- [ ] On session end, unbinds those vars

### Tests

- [ ] `core.observability.handle_metrics_event` unit test:
  - Given a sample `MetricsCollectedEvent`, asserts a JSON log line is emitted with the expected `event`, numeric fields, and bound context
- [ ] `core.observability` unit test verifying that bound contextvars appear on emitted log lines

### Demo

- [ ] Have a voice conversation
- [ ] `docker logs voice-ai-agent` (or the equivalent compose command) shows a `turn_metrics` JSON line per turn including total_latency in milliseconds
- [ ] The same log lines include `user_id` and `conversation_id`, allowing a user to grep the log for a single conversation
- [ ] `docs/agents` does **not** require updates; the README's "Observability" section documents how to grep for `turn_metrics` and notes that a future iteration will add Langfuse and a dashboard

## Blocked by

- Issue 05 (voice loop tracer)

## Comments

> *This was generated by AI during triage.*

**2026-05-04 — Bulk-promoted to `ready-for-agent`.** Maintainer reviewed the full set of 11 issues produced by `/to-issues` and approved them all for AFK pickup in one pass. The issue body above is treated as the agent brief; no separate brief was authored. Category labelled `enhancement`. Implementation order is governed by the `Blocked by` field — start by acquiring the issue with no remaining blockers, then move down the dependency chain.

**2026-05-04 — Implementation log (AFK agent).** Landed the minimum-viable monitoring slice on branch `issue-11`.

- `packages/core/core/observability.py` — additive extension (kept the issue-04 `setup_logging(settings)` signature intact since the AC said "verify it has the right shape; otherwise this is just a no-op extension"). Added `bind(**fields)` and `unbind(*keys)` thin wrappers over the structlog contextvars helpers, and `handle_metrics_event(event)` which projects whichever LiveKit `AgentMetrics` shape was wrapped (LLM / STT / TTS / EOU / RealtimeModel / VAD / Interruption) onto a single `event = "turn_metrics"` INFO-level log line. The handler is duck-typed (`getattr(event, "metrics", event)`) so it works against the real `MetricsCollectedEvent` and against test stand-ins. Numeric / identity field allow-lists are explicit so token-count and latency fields land on the line regardless of which sub-metric fired.
- `apps/agent/agent/session.py` — additive metrics hook delimited by a comment block (issue 07's parallel agent is touching the tool-wiring section above): `_wire_metrics_logging(session)` subscribes to `session.on("metrics_collected", ...)`, and the entrypoint binds `session_id` (room name) + `user_id` contextvars before `session.start(...)`, unbinding them in a `finally` so the contextvars don't leak between sessions on the same worker.
- `apps/api/api/middleware.py` — left as-is. The middleware already routes through `core.observability.bind_request_context` / `clear_request_context` (issue 04 wired this seam), which itself sits on top of `bind_contextvars`. Refactoring it to call the new `bind`/`unbind` aliases instead would be a churn-only change with no behavioural difference, so the AC's "if it uses a different mechanism, refactor" guard does not trigger.
- `setup_logging` signature — kept as `setup_logging(settings: Settings | None = None)`. The AC sketched a `(level, json_output)` shape but flagged the verify-or-noop branch; changing the signature would have rippled into both `apps/api` and `apps/agent` entrypoints for no functional gain (settings already carries `log_level` and `environment`, and the dev-vs-prod renderer choice is driven off `environment`).
- Tests — `packages/core/tests/unit/test_observability_metrics.py` (6 cases: LLM shape, EOU shape, contextvar pass-through, bare-metrics duck-typing, unknown shape, structlog wiring sanity) and `packages/core/tests/unit/test_observability_context.py` (4 cases: bound-vars on lines, unbind, no-op `bind()`, no-op `unbind` of unknown keys). Both files use a local `LogCapture` fixture configured with `merge_contextvars` because the default `structlog.testing.capture_logs` short-circuits the processor chain and would not exercise the contextvars path.
- README — added an "Observability" subsection under "Voice loop setup" with a sample `turn_metrics` line and the `docker logs … | grep turn_metrics` query, explicitly noting that Langfuse / dashboards / client-side TTFA are deferred.

Verification: `uv sync --all-packages`, `pnpm install`, `pnpm exec turbo run lint typecheck test` (12 / 12 tasks green; 45 core tests + 9 agent tests), `pnpm exec prettier --check .`, and `pnpm --filter @voice-ai/web build` all clean.
